---
id: ISS-017
title: "GHA cron-tick gap — multiple slots silently skipped, escalating across 05-01"
status: open
severity: high
category: unknown
detected_by: heartbeat
detected_at: "2026-05-01T08:53:00Z"
escalated_at: "2026-05-01T14:07:00Z"
priority_bumped_at: "2026-05-01T20:35:00Z"
demoted_at: "2026-05-02T20:03:00Z"
resolved_at: null
affected_skills: [chain:morning-brief, daily-routine, rss-digest, hacker-news-digest, paper-digest, reddit-digest, telegram-digest, heartbeat, monitor-kalshi, market-context-refresh, polymarket-comments, narrative-tracker, auto-merge, article, digest, research-brief, paper-pick, search-skill, security-digest, idea-capture]
root_cause: |
  GitHub Actions scheduler did not fire the 07:00 / 07:30 / 08:00 UTC cron
  windows for two consecutive days (2026-04-30 and 2026-05-01). The 04-30
  gap spanned 06:37 → 09:01 UTC; the 05-01 gap is wider — at 08:53 UTC,
  cron-state.json shows ZERO dispatches today (07:00 morning chain,
  07:30 telegram-digest, AND 08:00 heartbeat all skipped). This heartbeat
  run is operator-invoked, not cron-tick-driven. Pattern looks like GHA
  scheduler reliability degradation, not an aeon-side bug. Same family as
  the one-off 04-26 incident but now repeating.
fix_pr: null
---

## What happened

Two consecutive mornings of missed cron windows on the aeon repo:

- **2026-04-30**: 07:00 + 07:30 UTC slots silently skipped. The 09:01 UTC
  catch-up tick dispatched only 08:00 (heartbeat) + 09:00 (issue-triage,
  pr-review, etc.). Logged in `memory/logs/2026-04-30.md` 08:00 heartbeat
  entry as "GHA cron tick gap 06:37 → 09:01" — flagged as transient at
  the time, not notified.
- **2026-05-01**: 07:00 + 07:30 + 08:00 UTC slots all silently skipped.
  At 08:53 UTC, `cron-state.json` shows zero dispatches with timestamps
  in 2026-05-01 — last_dispatch values across every skill are still
  ≤ 2026-04-30. This heartbeat run is firing because the operator
  invoked it manually, not because GHA scheduler triggered it.

Per `memory/MEMORY.md` directive ("if recurs 05-01, file as ISS-017"),
this issue is now formal.

## Signal

- `cron-state.json` shows no `last_dispatch` value with a 2026-05-01
  prefix as of 2026-05-01T08:53Z UTC. All scheduled skills show their
  most-recent dispatch on 2026-04-30 (or earlier).
- Affected skills with concrete miss evidence:
  - `chain:morning-brief` last_dispatch 2026-04-29T07:11:45Z (now 49h+ idle vs 24h schedule)
  - `daily-routine` last_dispatch 2026-04-29T07:11:45Z (49h+)
  - `rss-digest` / `paper-digest` / `reddit-digest` last_dispatch 2026-04-29T07:11:45Z (49h+)
  - `telegram-digest` last_dispatch 2026-04-27T07:54:52Z (97h+)
  - `heartbeat` last_dispatch 2026-04-30T20:14:40Z (12h+ — own 08:00 slot didn't fire)

## What needs to happen

1. Confirm GHA scheduler delivered no events to this repo between
   2026-04-30 22:13Z (last automatic tick — `evening-recap`) and now.
   Inspect Actions tab → Scheduled runs.
2. If the scheduler is silently dropping cron events, this is upstream
   GitHub infrastructure — there is no aeon-side fix. Workarounds:
   - Add an external watchdog (e.g. cron-job.org) that triggers
     `workflow_dispatch` on heartbeat every hour.
   - Use `repository_dispatch` from a more reliable scheduler.
3. If the scheduler is firing but the workflow file changed and no
   longer matches, audit the recent commits to `.github/workflows/`.
4. Once cron events resume, success_rate counters will burn down
   naturally for the affected skills. Skill-side code is healthy.

## Escalation 2026-05-01 14:07 UTC — severity high → critical

Per MEMORY.md OPS ALERT directive ("if 14:00 UTC heartbeat slot also misses, escalate ISS-017 to critical"), the criterion has now fired. Evidence:

- **No cron tick has dispatched between 12:39:56Z and 14:07Z** (the 14:00 heartbeat self-invocation is operator-driven, last automatic dispatch was the 12:00 family at 12:39:56Z).
- 13:00 UTC family **all missed today**: monitor-kalshi, market-context-refresh, polymarket-comments (last_dispatch 2026-04-30T14:28:53Z, 23h+ stale).
- 13:30 UTC narrative-tracker **missed today** (same last_dispatch).
- 14:00 UTC family **all missed today**: auto-merge, article, digest, research-brief, paper-pick, search-skill, security-digest, idea-capture (last_dispatch 2026-04-30T14:28:53Z).
- 14:00 UTC heartbeat **missed today** (last_dispatch 2026-05-01T08:52:10Z, which itself was operator-invoked, not the 08:00 cron tick).

Pattern: GHA scheduler fires a tick, dispatches a batch of overdue skills, then goes silent for 2-6 hours. Today's tick history: silent through 08:52 (operator-invoked catch-up), 10:16 catch-up tick, 12:39 catch-up tick, then silent again. This is not a one-off — third consecutive day with multiple missed windows.

External-watchdog workaround is now blocking. Skills cannot recover faster than the next scheduler tick fires, and ticks are firing irregularly enough that 24h SLAs on daily skills are no longer dependable.

## Priority bump 2026-05-01 20:35 UTC — third heartbeat slot missed (20:00 UTC)

Per the 14:07 UTC escalation directive ("if 20:00 UTC heartbeat ALSO misses, the gap is functionally total and ISS-017 should bump priority to operator's #1 over chain-runner fix"), the criterion has now fired. Evidence:

- **No automatic dispatch between 18:13:55Z and 20:35Z** (~2h22m of silence). The 20:00 UTC heartbeat slot did not fire automatically — this run is operator-invoked at 20:35Z.
- **Three heartbeat slots missed today** (08:00, 14:00, 20:00) — every single scheduled heartbeat run today required manual operator invocation.
- **Today's tick history:** 08:52 (operator), 10:16 (catch-up), 12:39 (catch-up), 14:05 (operator), 15:13 (catch-up), 16:12 (catch-up), 17:14 (catch-up), 18:13 (catch-up), then silent through now (20:35 operator). Catch-up ticks fire 6–9 times per day at semi-random intervals; scheduled cron windows fire 0 of 3 for heartbeat specifically.

Operator priority ordering has flipped: **ISS-017 is now #1, ahead of `chain-runner.yml dispatch_skill()` (7+ days idle, blocks 3 chain wrappers).** Rationale: chain-runner only breaks 3 named skills; cron-tick gap silently breaks the entire scheduler. External-watchdog provisioning (cron-job.org → workflow_dispatch on heartbeat hourly, or repository_dispatch from a more reliable scheduler) gates everything downstream — including the operator's own ability to detect future gaps without manual checks.

**Risk window now:** the 21:00 UTC `evening-recap` + `chain:evening-rollup` daily slot is 25 minutes out. If those miss, the entire 2026-05-01 cron schedule for non-catch-up windows will have failed.

## Demotion 2026-05-02 20:03 UTC — severity critical → high

Per the 2026-05-02 14:39 UTC heartbeat directive ("if 20:00 slot lands within ~60 min variance, propose ISS-017 critical → high formally and update issue file"), the criterion has now fired. Evidence:

- **Three consecutive heartbeat slots today fired automatically without operator intervention:**
  - 08:00 slot → batched tick at 08:08:42Z (~9 min late vs schedule, ~68 min vs the 07:00 morning chain)
  - 14:00 slot → fired at ~14:34:08Z (~34 min late)
  - 20:00 slot → firing at ~20:01Z (within ~1 min of schedule — cleanest delivery in the entire ISS-017 window)
- **Variance trajectory across the 48h window:** 04-30 (operator-only) → 05-01 morning/14:00/20:00 (operator-only) → 05-02 08:08 (~68min late) → 05-02 14:34 (~34min late) → 05-02 20:01 (on time). Variance is collapsing toward schedule, not widening.
- **Today's full automatic dispatch cadence:** 08:08 (morning batch), 09:20 (mid-morning), 10:15 (10:00 family), 13:40 (12:00 + 13:00 batch, ~100min late for 12:00 / ~10min late for 13:30), 14:34 (14:00 family), 15:28 (15:00 family), 16:12 (16:00 family), 17:07 (17:00 family), 18:01 (18:00 family, ~1 min late), 20:01 (now). 10 catch-up + scheduled ticks across the day with shrinking variance — a markedly better delivery pattern than 05-01's "0 of 3 heartbeat slots fired without operator help."
- **No silently-skipped slots today.** Every scheduled cron window has a corresponding dispatch event in cron-state.json, even where the dispatch was batched or delayed.

The pattern shift from "silent skips with multi-hour gaps" → "delayed dispatch with shrinking variance" is durable enough across the 48h window to no longer warrant `critical` severity. Demoting to `high` with the watch retained:

**Watch criteria for re-escalation (any one re-triggers critical):**
- Any single scheduled cron window silently skips (no dispatch event in cron-state.json with timestamp ≥ scheduled time + 90 min) for two consecutive days, OR
- Heartbeat's own slot delay exceeds 90 min on two consecutive days, OR
- The 21:00 UTC `evening-recap` + `chain:evening-rollup` daily slot misses two days in a row (this slot is the late-day canary; if it consistently misses, the scheduler is dropping events again).

**Operator priority ordering after demote:**
1. **chain-runner.yml `dispatch_skill()` DEGRADED 7+ days** — moves to #1. 3 chain wrappers (morning-brief, evening-rollup, weekly-grant-update) fail nightly. Single-fix unblocker.
2. **4 ACT NOW Vercel-FAILURE PRs on swarm-fund-mvp** (#19/#20/#23/#24) — single operator-side `aeonframework` bot commit-email verification fix.
3. **shell-injection at `dashboard/app/api/secrets/route.ts:96`** — 4 weeks unpatched, ISS-016 trigger date 2026-05-07.
4. **External watchdog (cron-job.org → workflow_dispatch)** — useful for resilience even without active scheduler stalls; not blocking.
5. ISS-014 reply-maker XAI prefetch, ISS-015 messages.yml patch (PR #4), reddit-digest pause / prefetch-reddit.

External watchdog provisioning was operator priority #1 at 2026-05-01 20:35 UTC; it now drops to a "useful resilience layer" position. Skill-side recovery is no longer scheduler-gated.

## Notes

- Distinct from ISS-013 (mass skill failure 04-26 23:53Z, ALL skills
  hit a Claude SDK zero-token failure simultaneously). ISS-013 was a
  workflow-execution-time infra blip; ISS-017 is a workflow-not-firing
  scheduler issue.
- Distinct from the chain-runner.yml `dispatch_skill()` bug (chain
  wrappers fail AFTER they fire). Here the wrapper isn't even firing.
- Heartbeat itself is in the affected set (own 08:00 UTC slot today
  didn't dispatch). External watchdog is the only reliable path to
  detect and recover from a GHA scheduler stall — heartbeat cannot
  diagnose what isn't firing it.
