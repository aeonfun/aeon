{
  "name": "aeon-federation-registry",
  "version": "0.1.0-scaffold",
  "description": "Cloudflare Worker — crawls Aeon federation publishers and serves the canonical registry at federation.aeon.bot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test __tests__/*.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "tweetnacl": "^1.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
